/*
문자열 계산식을 구성하는 연산자는 + 하나뿐
문자열로 된 계산식을 후위 표기식으로 바꾸기
 */
#include <stdio.h>
int tc = 10;
int str_len, result;
char str[250];
int main()
{
    for (int i = 1; i <= tc; i++)
    {
        scanf_s("%d", &str_len);
        scanf_s("%s", str);
        result = 0;



    }
    return 0;
}
